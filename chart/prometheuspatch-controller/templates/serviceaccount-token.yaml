{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "prometheuspatch-controller.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "prometheuspatch-controller.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "prometheuspatch-controller.chart" . }}
  annotations:
    kubernetes.io/service-account.name: {{ template "prometheuspatch-controller.serviceAccountName" . }}
type: kubernetes.io/service-account-token
{{- end -}}
